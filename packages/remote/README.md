# remote

## Build Setup

```bash
# install dependencies
yarn

# serve with hot reload at localhost:3001
$ yarn start

# build for production
$ yarn build
```

```js
({
  type: 'Application',
  typeweb: 'SPA',
  toolsbuild: 'Webpack5',
  name: 'remote',
  port: '3001',
  framework: 'react',
  language: 'JavaScript',
  css: 'CSS',
})
```